guard TodoInput {
  text: string @minLen(1) @maxLen(200)
  email: string @email
}

out ui HomePage {
  head {
    title: "Test App"
  }

  <main>
    <h1>Hello from GaleX!</h1>
    <p>This page has a guard with @minLen, @maxLen, and @email validators.</p>
  </main>
}
